we have start opening with the text editor or notapad
next,we should import the packages like util
to hide the data we used the priavte key word
and also we use the run time polymorphism called method overridding
and we also use break and switch loops for the program
this program is used to take the data and write data and also used to import the data in the termineral as you can also see
as we used private key you we cannot easliy acessed by the class so we use get() and set() methods
